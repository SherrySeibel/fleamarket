<h3><%= @category.name %> Category</h3>

<% if current_user.admin %>
  <%= button_to "Delete Category", @category, method: :delete %>
  <p>CAUTION - Deleting this category will destroy all products assigned to it.</p>
<% end %>

<ol>
  <% @products.each do |product| %>
    <li><%= product.name %> | Auction ends at: <%= product.auction_ends_at %></li>
  <% end %>
</ol>
